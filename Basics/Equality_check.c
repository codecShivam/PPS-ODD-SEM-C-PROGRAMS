#include<stdio.h>
         //WAP TO FIND WEITHER TWO NUMBERS ARE EQUAL OR NOT
int main(){
    int a,b;
    printf("Enter the valur of a:\n");
    scanf("%d",&a);
    printf("Enter the valur of b:\n");
    scanf("%d",&b);
    if (a==b)
    {
        printf("a and b are equal");
    }
    else {
        printf("a and b are not equal");
    }
    return 0;
}